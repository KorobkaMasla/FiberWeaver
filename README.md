# FiberWeaver
Веб-приложение для документирования инфраструктуры кабельных сетей с интерактивной картой, редактированием схем волокон и управлением региональными данными.

## 📋 Требования

- **Python** 3.9+
- **Node.js** 16+
- **npm** 8+

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd XakatonSvyazi
```

### 2. Установка зависимостей Backend

```bash
# Создание виртуального окружения
python -m venv .venv

# Активация виртуального окружения
# Windows:
.\.venv\Scripts\Activate.ps1
# или
.\.venv\Scripts\activate.bat

# macOS/Linux:
source .venv/bin/activate

# Установка зависимостей
cd backend
pip install -r requirements.txt
```

### 3. Установка зависимостей Frontend

```bash
cd frontend
npm install
```

## ⚙️ Конфигурация

### Backend конфигурация

Backend использует SQLite для разработки. Конфигурация находится в `backend/app/core/config.py`:

```python
DATABASE_URL = "sqlite:///./test.db"  # Локальная БД
CORS_ORIGINS = ["http://localhost:5173", "http://localhost:3000"]
```

Для миграции на PostgreSQL в production:
1. Обновите `DATABASE_URL` в `config.py`
2. Установите `psycopg2-binary`: `pip install psycopg2-binary`

## 🏃 Запуск приложения

### Способ 1: Раздельный запуск (рекомендуется для разработки)

#### Запуск Backend

```bash
cd backend
python -m uvicorn app.main:app --reload --port 8000
```

Backend будет доступен по адресу: **http://localhost:8000**
- API документация: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

#### Запуск Frontend (в отдельном терминале)

```bash
cd frontend
npm run dev
```

Frontend будет доступен по адресу: **http://localhost:5173**

### Способ 2: Использование скриптов

#### Windows (PowerShell)

```bash
# Запуск обоих сервисов
.\start.bat

# или отдельно:
.\start.sh          # Frontend
.\start_backend.bat # Backend
```

#### macOS/Linux

```bash
# Запуск обоих сервисов
./start.sh

# или отдельно:
./start_backend.sh  # Backend
npm run dev         # Frontend
```

## 📁 Структура проекта

```
XakatonSvyazi/
├── backend/
│   ├── app/
│   │   ├── core/              # Конфигурация и безопасность
│   │   ├── database/          # Подключение к БД
│   │   ├── models/            # SQLAlchemy ORM модели
│   │   ├── schemas/           # Pydantic валидация
│   │   ├── routes/            # API endpoints
│   │   └── utils/             # Утилиты
│   ├── requirements.txt       # Python зависимости
│   └── init_db.py             # Инициализация БД
│
├── frontend/
│   ├── src/
│   │   ├── components/        # React компоненты
│   │   ├── services/          # API клиент
│   │   ├── styles/            # CSS стили
│   │   └── App.jsx            # Главный компонент
│   ├── package.json
│   └── vite.config.js
│
└── docs/                      # Документация проекта
```

## 🗄️ База данных

### Инициализация БД

Backend автоматически инициализирует БД при запуске:
1. Создает все необходимые таблицы
2. Загружает справочные данные (типы кабелей и объектов)
3. При необходимости создает пользователей по умолчанию

### Текущие таблицы

- `users` - пользователи системы
- `network_objects` - объекты сети (узлы, муфты, шкафы и т.д.)
- `cables` - кабельные соединения
- `fiber_splices` - сварки волокон
- `cable_types` - справочник типов кабелей
- `object_types` - справочник типов объектов
- `regions` - географические регионы
- `region_objects` - связь объектов с регионами
- `region_cables` - связь кабелей с регионами

## 🔐 Аутентификация

API использует JWT токены для аутентификации:

```bash
# Получить токен (пример)
curl -X POST "http://localhost:8000/api/auth/token" \
  -H "Content-Type: application/json" \
  -d '{"username": "admin", "password": "admin"}'
```

Добавить токен в заголовки:
```
Authorization: Bearer <token>
```

## 🛠️ Основные API endpoints

### Сетевые объекты
- `GET /api/network-objects/` - список объектов
- `POST /api/network-objects/` - создать объект
- `PUT /api/network-objects/{id}` - обновить объект
- `DELETE /api/network-objects/{id}` - удалить объект

### Кабели
- `GET /api/cables/` - список кабелей
- `POST /api/cables/` - создать кабель
- `PUT /api/cables/{id}` - обновить кабель
- `DELETE /api/cables/{id}` - удалить кабель

### Справочники
- `GET /api/reference/cable-types` - типы кабелей
- `GET /api/reference/object-types` - типы объектов

### Регионы
- `GET /api/regions/` - список регионов
- `POST /api/regions/` - создать регион
- `GET /api/regions/{id}` - регион с объектами и кабелями
- `DELETE /api/regions/{id}` - удалить регион

Полная документация: http://localhost:8000/docs

## 🎨 Frontend возможности

### Главные компоненты

**MapEditor** - интерактивная карта с инструментами:
- 📍 Добавление объектов на карту
- 🔗 Рисование кабельных соединений
- 🎨 Режим рисования (полигоны, линии, круги)
- 📏 Измеритель расстояний
- 🔍 Поиск по адресу

**SchemaEditor** - редактор схем волокон:
- 📊 Визуализация кабельных схем
- ✂️ Управление сварками волокон
- 🔍 Поиск по названию кабеля и адресу

**RegionFilter** - фильтр по регионам:
- 🗺️ Выбор регионов из списка
- 🔍 Поиск по Nominatim (OpenStreetMap)
- 📍 Добавление регионов на карту

### Горячие клавиши

| Клавиша | Действие |
|---------|----------|
| **M** | Переключить режим измерения |
| **K** | Переключить режим рисования кабеля |
| **P** | Переключить режим добавления точки |
| **C** | Очистить измерения/выделения |
| **Esc** | Отменить текущий режим |
| **Space** | Панорамирование карты (в режиме рисования) |

## 🧪 Тестирование API

### Использование curl

```bash
# Получить список объектов
curl -X GET "http://localhost:8000/api/network-objects/" \
  -H "Authorization: Bearer <token>"

# Создать новый объект
curl -X POST "http://localhost:8000/api/network-objects/" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <token>" \
  -d '{
    "name": "Узел 1",
    "object_type": "node",
    "latitude": 55.7558,
    "longitude": 37.6173,
    "address": "Москва, Россия"
  }'
```

### Использование Swagger UI

Откройте http://localhost:8000/docs в браузере для интерактивного тестирования всех endpoints.

## 📦 Экспорт и импорт

### Экспорт данных

```bash
# Экспорт в JSON
GET /api/export/json

# Экспорт в CSV
GET /api/export/csv

# Экспорт в GeoJSON
GET /api/export/geojson
```

### Импорт данных

```bash
# Импорт из JSON
POST /api/import/json
Body: file (JSON)

# Импорт из CSV
POST /api/import/csv
Body: file (CSV)
```

## 🐛 Решение проблем

### Backend не запускается

```bash
# Проверьте что Python установлен
python --version

# Переустановите зависимости
pip install -r requirements.txt --upgrade

# Очистите кэш Python
find . -type d -name __pycache__ -exec rm -r {} +
```

### Frontend не запускается

```bash
# Проверьте что Node.js установлен
node --version
npm --version

# Очистите npm кэш
npm cache clean --force

# Переустановите зависимости
rm -rf node_modules package-lock.json
npm install
```

### БД не инициализируется

```bash
# Удалите старую БД
rm backend/test.db

# Перезапустите backend
# БД автоматически пересоздастся при запуске
```

### CORS ошибки

Проверьте что `CORS_ORIGINS` в `backend/app/core/config.py` содержит адрес frontend:
```python
CORS_ORIGINS = ["http://localhost:5173"]  # для разработки
```

## 📝 Переменные окружения

Создайте файл `.env` в корне backend (опционально):

```env
DATABASE_URL=sqlite:///./test.db
SECRET_KEY=your-secret-key-here
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
CORS_ORIGINS=["http://localhost:5173"]
```

## 🚢 Производство (Production)

### Backend

```bash
# Используйте production ASGI server (gunicorn)
pip install gunicorn

# Запуск с 4 worker процессами
gunicorn -w 4 -k uvicorn.workers.UvicornWorker app.main:app
```

### Frontend

```bash
# Сборка production версии
npm run build

# Результат будет в папке dist/
# Раздайте файлы через nginx или другой веб-сервер
```

### Docker

```bash
# Сборка контейнера
docker build -t cable-network-app .

# Запуск контейнера
docker run -p 8000:8000 -p 5173:5173 cable-network-app
```

## 📚 Дополнительные ресурсы

- [FastAPI документация](https://fastapi.tiangolo.com/)
- [React документация](https://react.dev/)
- [Leaflet.js документация](https://leafletjs.com/)
- [SQLAlchemy документация](https://docs.sqlalchemy.org/)
- [Tailwind CSS документация](https://tailwindcss.com/)

## 👥 Контактная информация

Для вопросов и предложений создавайте Issues в репозитории.

---

**Версия**: 1.0.0  
**Дата обновления**: Ноябрь 2025

